apiVersion: v1
kind: Service
metadata:
  name: db-ontology-service
  namespace: db-ontology
spec:
  selector:
    app: db-ontology-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
